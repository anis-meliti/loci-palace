"""
loci-palace - a structured, self-validating Markdown vault that gives AI
assistants persistent, routable memory.

Named for the method of loci: memory anchored to places you can navigate back
to. The vault is the places; the manifest is the map.

The package ships machinery only. It contains no notes, no identity, and no
project data, and it never will: content lives in the vault a user scaffolds,
never in the distribution. That separation is structural here rather than
enforced by an allowlist, which is the main reason for packaging at all.
"""

__version__ = "0.1.0"
